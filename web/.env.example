# Public browser configuration only. Never put secrets in NEXT_PUBLIC_* values.
NEXT_PUBLIC_API_BASE_URL=http://127.0.0.1:8000

# Used only by `npm run api:schema:pull`.
CROPTWIN_OPENAPI_URL=http://127.0.0.1:8000/openapi.json

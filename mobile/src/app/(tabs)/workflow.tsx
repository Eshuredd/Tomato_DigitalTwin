import { FutureArea } from '@/features/foundation/future-area';
export default function WorkflowScreen() { return <FutureArea testID="screen-workflow" eyebrow="Daily review" title="Workflow" description="A compact stage progression will guide evidence review and deterministic decisions." availableWhen="Workflow stages become available after an authoritative active cycle has been selected." />; }
